# VitaNova API
fastapi==0.141.1
uvicorn[standard]==0.52.1
# Native async driver (AsyncMongoClient); Motor is EOL. dnspython comes with it
# as a hard dependency, so mongodb+srv:// URIs -- i.e. every MongoDB Atlas
# connection string -- resolve without an extra. (The [srv] extra was dropped in
# PyMongo 4.9; asking for it now only earns a warning.)
pymongo==4.17.0
pydantic[email]==2.13.4
pydantic-settings==2.15.0
PyJWT==2.13.0
bcrypt==5.0.0
Jinja2==3.1.6
weasyprint==69.0           # needs pango + cairo (brew install pango)
python-multipart==0.0.32

# dev / test
pytest==9.0.1
pytest-asyncio==1.3.0
httpx==0.28.1
